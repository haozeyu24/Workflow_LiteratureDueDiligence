# Notes

This is an example run that demonstrates how the reusable workflow is instantiated for a concrete biological question.

It should not be treated as part of the fixed workflow specification.

The reusable workflow remains in the root files:

- `workflow.md`
- `policy.md`
- `schemas/`
- `templates/`
